name: Sync Google Scholar Publications

on:
  schedule:
    # Run on the 1st of every month at 00:00 UTC
    - cron: '0 0 1 * *'
  workflow_dispatch:  # Allow manual trigger

jobs:
  sync-publications:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Fetch publications from Google Scholar
        run: |
          python scripts/sync_scholar.py

      - name: Smart merge publications (keep old + add new + update citations)
        run: |
          python scripts/smart_merge_publications.py

      - name: Extract and update collaborators
        run: |
          python scripts/extract_collaborators.py

      - name: Commit and push if changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/publications.json data/collaborators.json publications.html
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-update publications and collaborators from Google Scholar [$(date +'%Y-%m-%d')]" && git push)
